Implement Epic 11: DevOps and Docker based on AGENTS.md and the existing repository.

Build the next engineering capability for the `ai-assisted-architecture-governance` repository: Dockerized Local Development, CI Validation, and DevOps Foundation.

Before implementation:

1. Read `AGENTS.md`.
2. Review the current repository structure.
3. Review the implementation from Epic 0 through Epic 10.
4. Review existing backend services, Angular frontend, prompt catalog, mock AI provider, Azure OpenAI provider readiness, artifact generation, review workflow, tests, documentation, observability setup, and Azure-readiness notes.
5. Preserve the existing architecture direction, responsible AI guidance, AI provider abstraction, safe AI telemetry rules, security standards, testing standards, observability standards, and Azure deployment direction.

Epic 11 Goal:

Make the platform easy to build, test, run, and validate using Docker and GitHub Actions.

This epic should demonstrate enterprise delivery maturity, repeatable builds, local developer experience, CI quality gates, containerization readiness, and Azure Container Apps deployment readiness.

Do not deploy to Azure in this epic. Prepare the project for future Azure deployment.

Backend Docker scope:

1. Add or standardize Dockerfiles for backend API projects.
2. Use multi-stage Docker builds where practical.
3. Ensure backend services can run in containers.
4. Ensure containers use environment-based configuration.
5. Ensure the mock AI provider remains the default local provider.
6. Ensure Azure OpenAI configuration remains optional.
7. Ensure services expose documented ports.
8. Ensure health check endpoints are usable inside containers.
9. Do not hardcode secrets.
10. Do not include development-only secrets inside images.
11. Optimize Dockerfiles for clean, repeatable builds.
12. Add `.dockerignore` files where appropriate.

Backend services/projects to consider:

* `ArchitectureGovernance.Api`
* `ArchitectureGovernance.Application`
* `ArchitectureGovernance.Domain`
* `ArchitectureGovernance.Infrastructure`
* `ArchitectureGovernance.AI.Abstractions`
* `ArchitectureGovernance.AI.Mock`
* `ArchitectureGovernance.AI.AzureOpenAI`

Only create containers for executable services. Class library projects should be included as dependencies, not separate containers.

Frontend Docker scope:

1. Add Dockerfile for Angular frontend.
2. Use production-style Angular build.
3. Serve the built Angular app using a lightweight web server such as Nginx if appropriate.
4. Keep environment configuration flexible for API base URLs.
5. Do not hardcode production backend URLs.
6. Add `.dockerignore` for frontend if appropriate.

Docker Compose scope:

1. Update or create `docker-compose.yml`.
2. Add services for backend API, frontend, and SQL Server.
3. Add local networking between services.
4. Add environment variables for local development.
5. Default AI provider should be `Mock`.
6. Add safe Azure OpenAI placeholder environment variables only if useful.
7. Add health checks where practical.
8. Add named volumes for database persistence.
9. Add clear startup dependencies where practical.
10. Ensure the full system can be started locally with one command.

Suggested Docker Compose services:

* `architecture-governance-api`
* `architecture-governance-portal`
* `sqlserver`

Optional services only if the repository already supports them cleanly:

* `azurite`
* `seq`
* `redis`

Do not add unnecessary infrastructure if it does not serve the current MVP.

Local developer experience scope:

1. Add clear local run commands.
2. Add clear Docker run commands.
3. Add clear database migration commands.
4. Add clear test commands.
5. Add troubleshooting notes.
6. Add environment variable documentation.
7. Add or update `.env.example` with safe placeholder values.
8. Ensure local setup does not require Azure credentials.
9. Ensure local setup does not require Azure OpenAI credentials.
10. Ensure mock provider works by default.

GitHub Actions CI scope:

Update or create `.github/workflows/ci.yml`.

The CI pipeline should:

1. Trigger on pull requests and pushes to main.
2. Restore backend dependencies.
3. Build backend solution.
4. Run backend tests.
5. Install frontend dependencies.
6. Run frontend linting if available.
7. Build Angular frontend.
8. Run frontend tests where practical.
9. Validate Docker builds where practical.
10. Keep the workflow readable and maintainable.
11. Avoid requiring Azure OpenAI credentials.
12. Avoid requiring Azure credentials.

Optional but useful CI improvements:

* Separate jobs for backend, frontend, and Docker validation.
* Dependency caching.
* Test result visibility.
* Build summary.
* Path-based organization if the repository is large.

Do not add complex deployment automation yet. That belongs to the Azure deployment epic.

Quality gate expectations:

The repository should clearly support these validation commands:

* Backend restore
* Backend build
* Backend test
* Frontend install
* Frontend build
* Frontend test where practical
* Docker Compose startup
* Docker image build
* Bicep validation readiness if existing IaC already exists

Documentation scope:

Update or create relevant documentation:

* `README.md`
* `docs/setup.md`
* `docs/local-development.md`
* `docs/testing-strategy.md`
* `docs/deployment.md`
* `docs/devops-guide.md`
* `docs/azure-openai-provider.md`
* `docs/operational-runbook.md`
* `docs/roadmap.md`
* `architecture/deployment-architecture.md`
* `architecture/observability-architecture.md`
* `architecture/security-architecture.md`
* `architecture/nfrs.md`

Create `docs/devops-guide.md` if it does not exist.

The DevOps guide should include:

1. Local development without Docker.
2. Local development with Docker Compose.
3. Required environment variables.
4. Mock AI provider configuration.
5. Optional Azure OpenAI provider configuration using safe placeholders.
6. Database container usage.
7. Migration guidance.
8. How to run tests.
9. How CI is structured.
10. How Docker images map to future Azure Container Apps.
11. Troubleshooting common local setup issues.
12. No-secrets-in-repository policy.

Azure-readiness expectation:

Document how this DevOps and Docker foundation maps to future Azure resources:

* Azure Container Registry
* Azure Container Apps
* Azure SQL Database
* Azure Static Web Apps or Azure Storage Static Website
* Azure OpenAI
* Azure Key Vault
* Azure Application Insights
* Azure Log Analytics
* GitHub Actions deployment workflows

Security and responsible AI expectations:

1. Do not hardcode secrets.
2. Do not commit real `.env` files.
3. Use `.env.example` only with safe placeholders.
4. Do not require real Azure OpenAI credentials for local Docker.
5. Do not expose AI provider secrets to frontend containers.
6. Keep `AI_PROVIDER=Mock` as the default for local and CI.
7. Document how to supply Azure OpenAI configuration securely outside Git.
8. Ensure logs do not include full prompts, full requirement text, or full AI responses by default.

Architecture expectations:

1. Keep Docker and DevOps configuration clean and understandable.
2. Avoid unnecessary complexity.
3. Do not mix deployment secrets with application code.
4. Use environment variables for runtime configuration.
5. Keep containers small and production-oriented where practical.
6. Keep local Docker setup developer-friendly.
7. Preserve service boundaries.
8. Preserve AI provider abstraction.
9. Do not implement unrelated business features in this epic.

Testing expectations:

Add or update tests and validation where practical:

* CI builds backend successfully.
* CI runs backend tests.
* CI builds Angular frontend.
* CI validates Dockerfile builds if practical.
* Docker Compose configuration is syntactically valid.
* Health endpoints remain functional in containerized mode where practical.
* Mock provider remains default in Docker and CI.

Docker validation expectation:

Where practical, provide commands for:

```bash
docker compose build
docker compose up
docker compose down
```

Do not assume the user has Azure credentials.

Do not assume the user has Azure OpenAI credentials.

Important rules:

* Do not hardcode secrets.
* Do not commit real `.env` files with sensitive values.
* Do not require real Azure resources.
* Do not require Azure OpenAI credentials.
* Do not implement Azure deployment yet.
* Do not overbuild beyond Epic 11.
* Keep the project Azure-ready.
* Keep the implementation portfolio-grade and Solution Architect quality.

After implementation, provide:

1. What was implemented.
2. Docker files created or modified.
3. Docker Compose services added or modified.
4. GitHub Actions changes.
5. Backend build/test validation.
6. Frontend build/test validation.
7. Documentation updated.
8. Commands to run locally without Docker.
9. Commands to run locally with Docker.
10. Commands to test.
11. Mock AI provider validation.
12. Any assumptions made.
13. Any known limitations.
14. Recommended next epic.
